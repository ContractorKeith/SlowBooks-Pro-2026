# Worklog

### 2026-09-10 14:38 EDT - v2.10.2 Mac release review

- Reviewed upstream tag `v2.10.2`, commit `ebfd281eb807ccb93386defd5b27a5ea00e00f54`, on macOS 26.6.2 arm64. Source tests used Python 3.13.15 with Homebrew native PDF libraries. The public app bundles Python 3.12.10.
- Original release suite: 2,029 passed, one failed, no skips, 81% app coverage in 212.72 seconds. The failure at `tests/test_ocr.py:687` reproduced alone: clearing PATH does not hide `/opt/homebrew/bin/tesseract` from the intentional stock-location fallback. Fixed only test isolation by mocking the fallback candidates for its missing-install case. The later fake-install assertions remain intact. Focused regression now passes.
- Public versioned and stable DMGs both matched the manifest SHA-256 `1378b92dfc104bd6b71a47fbfe89fb5ddbf84023cc020e72800ce4111370398c`. Disk-image integrity, recursive strict app code-signature verification, Gatekeeper acceptance as Notarized Developer ID, and stapler validation on both app and DMG passed. Release build metadata matches the tag SHA. Independent bundle architecture/library audit passed for 150 Mach-O files.
- Ran the shipped executable's smoke test directly from the mounted image: Cocoa backend import, company creation/migration, HTTP health, PDF rendering, and Vision availability passed. Copied the exact app into a temporary Applications-equivalent directory, ejected the image, and launched its native UI. Created a disposable company, renamed control account 1000 through the form, verified disabled number/type fields and explanatory text, and inspected ordinary editable fields and light/dark appearance. Quit, relaunched, reopened the company, and verified the renamed account persisted.
- Against that copied app, logo upload and balance-sheet PDF generation passed; the PDF contains the 240x80 test logo image. Account CSV export includes the renamed account. Backup creation and download passed; downloaded SQLite backup passed integrity_check and retained the renamed account. Native Vision scanned all three receipt PDF fixtures and returned the expected totals 76.93, 52.20, and 39.31. No fatal or missing-library errors appeared in its launcher log; the app signature remained valid after use.
- Also remounted the public DMG, launched its native UI directly, opened the QA company, and verified the v2.10.2 dashboard. Quit and ejected the image afterward.
- Test data was isolated with `--data-dir`; existing installation and company books were not changed. This review did not test Intel, Windows, Linux, an upgrade of real books, or data storage at the default user directory.
- Evidence: local `~/Downloads/SlowBooks-Mac-QA-2.10.2/`. App/test data and exact-tag worktree remain in `/tmp/slowbooks-mac-evidence-2.10.2/` and `/tmp/slowbooks-mac-2.10.2/`. Both app instances opened during review were closed.
- Final checks: full suite with the test-isolation fix passed, 2,030 passed, no skips, 32 warnings, 81% coverage in 204.82 seconds. `black --check app/ tests/ scripts/`, `ruff check app/ tests/ scripts/`, and `git diff --check` passed. No application-code defect found in the completed Mac checks.
- Next: upstream can adopt the two-line test-isolation fix. This review does not replace owner acceptance of real company data.
